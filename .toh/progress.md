# Progress Ledger

> Append-only, one line per state change: `queued → running → done/failed/blocked`.
> Format: `YYYY-MM-DD HH:MM T00x <state> — <detail>` · gotchas as `LEARNING: <one line>`.
> Written by THE TOH LOOP (orchestration-protocol skill). Never rewrite history — append.

2026-07-28 18:50 plan.md archived — reports-v3 (done, CI green) + vt900 → .toh/memory/archive/
2026-07-28 18:50 new plan drafted by /toh-plan — map deep zoom + continuous replay + persistent login (4 tasks, Status: draft)
2026-07-28 18:58 T003 adjusted by user choice — hybrid storage: checkbox ติ๊ก = localStorage 30 วัน / ไม่ติ๊ก = sessionStorage (plan.md ยัง Status: draft รอ Go)
